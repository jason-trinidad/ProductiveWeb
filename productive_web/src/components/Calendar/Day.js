import React from 'react';

// Todo:
// 1. Implement 
// 1. Implement "all-day" section
// 2. Create enums for constants?
// 3. Import from Google Calendar

const Day = (props) => {
    const firstHour = 0;
    const hoursInDay = 24;
    const today = props.date;

    return (
        <>
            {/* Display 24 hours */}

            {/* How to default to a certain time? */}
            {/* Loop through hours */}
        </>
    )
};

export default Day;